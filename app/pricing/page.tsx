import { ContentPage, PricingCards } from '@/components/content-page'
export default function Pricing() { return <ContentPage eyebrow="Pricing" title="Start free. Pay only when you want the full report." intro="One-time payment. No subscription. Start with an initial visibility check, then decide whether the complete report is worth unlocking."><PricingCards /></ContentPage> }
