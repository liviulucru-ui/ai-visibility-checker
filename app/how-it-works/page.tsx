import { ContentPage } from '@/components/content-page'
import { CTA } from '@/components/marketing'
const steps = ['Enter your business', 'We generate relevant commercial-intent queries', 'We collect available real search evidence', 'We compare your business with competitors', 'We calculate the factual visibility result', 'We generate prioritized recommendations', 'You can unlock the complete report for $19']
export default function HowItWorks() { return <ContentPage eyebrow="How it works" title="A practical read on whether buyers can find you in AI-powered search." intro="The audit is designed around commercial questions—not vanity metrics or unsupported claims."><div className="grid gap-4">{steps.map((step, i) => <div key={step} className="flex gap-5 rounded-2xl border border-border bg-card p-6"><span className="flex size-8 shrink-0 items-center justify-center rounded-full bg-accent font-mono text-sm font-bold text-accent-foreground">{i + 1}</span><p className="font-semibold leading-6">{step}</p></div>)}</div><div className="mt-10"><CTA>Check My AI Visibility — Free</CTA></div></ContentPage> }
